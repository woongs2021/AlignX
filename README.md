# AlignX

**데이터로 검증하는, 모든 직무의 합격 포트폴리오**
기획자·PM·마케터·디자이너부터 코드리뷰까지 — AlignX가 1차로, 현직 멘토가 2차로 검증하는 3단계 AX(AI
Transformation) 검증 웹 서비스.

> **현재 상태: Phase 00–14 구현 완료.**
> **Page (GitHub Pages):** https://woongs2021.github.io/AlignX/
> `main` 브랜치에 push되면 GitHub Actions가 자동으로 재배포한다. 상세 계획은 `Plans/`.

---

## 최근 업데이트

### 2026-09-05

- **MY 페이지 카드 그리드 통일** — 회차가 1개일 때만 쓰던 3카드 요약 레이아웃을 없애고, 회차 수와 무관하게 항상 번호가 매겨진 카드 그리드로 통일. "3회부터 열립니다" 이력 잠금 안내 문구는 삭제
- **MY 상세 뷰 강화** — 최종 제출 전인 회차는 상세에서 실시간 멘토 검증 현황판을 그대로 보여주고(최신 회차만이 아니라), 멘토·리포트 요약 카드에 원칙별 점수 그래프·멘토 코멘트·AI-멘토 점수차 해설을 추가
- **3단계 리포트 정확도 수정** — "멘토 종합 코멘트"에서 실제 검증 멘토와 무관한 가짜 3인 페르소나 카드를 없애고 실제 멘토 코멘트만 표시. AI-멘토 점수차 해석 기준을 5점 → 3점으로 낮추고 문구에 기준을 명시
- **계정 메뉴 — 멘토 검증 요청 현황** — 로그인 계정에게 걸린 멘토 검증 요청 건수(진행 중+완료 포함)를 배지로 표시(로그인 전에는 숨김). 1건 이상이면 배지를 클릭해 바로 이동 가능 — 2건 이상은 MY 대시보드, 1건이면 상세(멘티) 또는 검토 화면(멘토)으로 점프. 계정 전환 시 홈으로 이동하도록 변경
- **1단계 결과 화면 흔들림 버그 수정** — 총점 카운트업 애니메이션 중 자릿수 폭이 바뀔 때마다 텍스트가 줄바꿈을 반복해 화면 전체가 흔들리던 버그 수정
- **HOME 히어로 모바일·태블릿 여백 수정** — 마퀴 카드 크기(모바일 220px, 데스크톱 320~400px)는 그대로 두고, 3행 이미지 블록을 상단 내비게이션 바로 아래 붙이고 히어로 높이에 상한을 둬서 세로로 긴 화면(모바일, 태블릿 세로, 큰 창)에서 생기던 하단 빈 여백을 줄임
- **멘토 검증 요청 폼** — 로그인 계정이 있으면 이름 필드를 계정 이름으로 고정해 수정 불가 처리
- **멘토 검증 큐** — "검증하기" 버튼 강조, 페이지 폭을 다른 탭과 통일
- **포트폴리오 분석 카피 수정** — 하단 CTA 카드 문구를 AI 분석 소요 시간 안내로 교체
- **CI 배포 안정화** — react-hooks lint 규칙 위반으로 막혀 있던 GitHub Actions 배포 파이프라인 수정

### 2026-09-04

- **계정 로그인 도입** — 비밀번호 없는 데모 계정 3종(멘티 · 멘토 · 관리자)을 상단 우측 로그인 아이콘에서 바로 전환. 로그인하면 아이콘이 프로필 썸네일로 바뀌고, 클릭 시 계정 전환·알림·로그아웃 메뉴가 뜬다. 역할에 따라 내비게이션도 달라진다(멘티·멘토는 MY, 관리자는 ADMIN만 노출)
- **멘토 검증 실연동** — 기존에는 타이머가 끝나면 더미 피드백이 자동 생성됐지만, 이제는 멘토 계정이 실제로 로그인해 MY의 "검증 요청 큐"에서 제출물을 열고, 페이지 뷰어로 전 페이지(PDF 최대 10장)를 넘겨보며 원칙별 0–10점과 종합 코멘트를 직접 작성해야만 3단계가 열린다
- **알림 시스템** — HOME을 포함한 전 화면에 알림 벨 추가(다크모드 토글은 HOME에서만 있던 비활성 상태를 없애고 완전히 숨김). 멘토 배정·검증 시작·검증 완료·최종 제출 시점마다 알림이 쌓이고, MY 하위 "알림" 서브탭에서 전체 기록 확인 가능
- **프로필 이미지** — 멘티·멘토는 사람 얼굴, 관리자는 로봇 얼굴로 AI 생성한 전용 프로필 사진 적용(`public/accounts/`)

### 2026-09-02

- **탭별 콘텐츠 폭 통일** — 포트폴리오 분석·MY는 넓게(1280px), AlignX·ABOUT·ADMIN은 좁게(1000px, 신규 `.container-narrow`) 고정. 상단 히어로 타이틀 크기·마진(폰트, eyebrow 간격, 상단 패딩)도 4개 탭 전부 동일하게 통일
- **라우트 전환 스크롤 버그 수정** — 탭 클릭 시 포커스 이동이 sticky 헤더를 고려하지 못해 스크롤이 0이 아닌 64px에서 멈추던 문제를 `focus({ preventScroll: true })`로 수정. 상단 네비게이션 구분선도 스크롤 여부와 무관하게 항상 표시
- **HOME 히어로 이미지 전면 교체** — 마퀴에 쓰이던 이미지 10장을 18장으로 교체하고 3행 × 6장 구성으로 재편(`HeroMarquee`가 이미지 풀을 행 수만큼 균등 분할)
- **접근성 스킵 링크 제거** — `top:-48px`로 숨겨야 했으나 실제 렌더링 높이가 이를 초과해 로고 위로 살짝 삐져나오던 "본문 바로가기" 링크를 삭제
- **CTA/배지 컬러 정리** — HOME·AlignX 하단 CTA 밴드 배경을 연한 톤으로, 버튼은 진한 강조색으로 교체하고 좌상단·우하단에 배너 장식 추가. AlignX FAQ 토글 배지와 5단계 파이프라인의 마지막 단계 색상도 CTA 버튼과 동일 계열로 정리
- **멘토 검증(2단계) 화면 정리** — 전역 리스트 리셋과의 우선순위 충돌로 어긋나 있던 단계 넘버링 좌측 정렬 수정, 진행 중 카드가 넘버링과 상단 정렬되도록 수정, 검증 완료 버튼을 강조 색상으로 변경
- **ABOUT 히어로** — 우측 상단에 불필요하게 덮여 있던 색면 프레임을 걷어내고 로고 이미지만 노출

### 2026-08-31

- **포지셔닝 전면 재작성** — "디자이너 전용" → 기획·PM·마케팅·디자인·개발(코드리뷰)까지 전 직무 대상 서비스로 전 페이지 카피 변경. AlignX 1차 검증 → 멘토 2차 검증 → 3단계 결과 배포 프로세스, B2B(교육기관) → B2C, 국내 → 해외 로드맵을 명시
- **분석 시작 플로우 재구성** — PORTFOLIO 인트로에 "분석 시작하기" 팝업 신설(직군 선택 → 파일 업로드 → 확인 → 전체화면 로딩). 로딩은 30초로 단축, 10대 원칙이 하나씩 클리어되는 애니메이션 · 진행률(%) · 라인+바 콤보 차트로 연출
- **만족도 설문을 3단계로 이동** — 기존에는 2단계 멘토 요청 제출 시 같이 받던 만족도 7점 척도·주관식 후기를, 3단계 통합 리포트를 확인한 뒤 "최종 포트폴리오 제출" 시점에 받도록 분리(`Attempt.finalReview` 필드 신설). 2단계는 이름·주제·요청사항 3항목만 남김
- **MY 페이지** — 3단계 완료 판정을 최종 제출 여부 기준으로 정교화, 하단 여백 수정, 로컬 데이터를 한 번에 지우는 "초기화" 버튼 추가(확인 팝업 포함)
- **ABOUT 페이지** — AlignX AI와 중복되던 자주 묻는 질문 섹션 삭제. 검증 멘토를 3인(디자인) → 6인(기획·마케팅·디자인·개발)으로 확대하고, 카드 클릭 시 학력·경력 예시를 보여주는 팝업과 애니메이션풍 프로필 이미지를 추가. 문의하기 문구를 "데모 목적" → "B2B로 운영 중"으로 수정
- **다크 모드 시인성 전수 수정** — 전 페이지 텍스트 대비 문제 조사·수정(`--accent-text`/`--mute` 토큰 정비), HOME은 다크 모드 토글을 비활성화
- **ADMIN** — 비밀번호 입력창 자동 포커스 + 안내 문구 추가, 라우트 전환 시 포커스를 가로채던 버그 수정

---

## 스크린샷

<!-- TODO: 배포 후 실제 화면 캡처로 교체 — HOME 히어로 / 1단계 분석 결과 / 3단계 통합 리포트.
     이 초안은 브라우저를 직접 띄울 수 없는 환경에서 작성돼 실제 캡처를 대신 첨부하지 못했다. -->

| HOME 히어로 | 1단계 분석 결과 | 3단계 통합 리포트 |
|---|---|---|
| _배포 후 추가 예정_ | _배포 후 추가 예정_ | _배포 후 추가 예정_ |

---

## 무엇을 만드는가

포트폴리오 피드백은 대개 "느낌"에 의존한다. 같은 포트폴리오를 두 심사자가 정반대로 평가하는 일이 흔하다 —
기획자든 마케터든 디자이너든, 심지어 코드리뷰조차 마찬가지다. AlignX는 정교하게 튜닝된 자체 LLM 시스템으로,
그 판단을 **10개 축으로 분해해 정량화**하고, 그 위에 **사람 멘토의 검증**을 덧대 한 장의 리포트로 묶는다.

```
포트폴리오 업로드
      ↓
① AlignX 1차 검증   10대 원칙 채점 → 총점 0–100
      ↓
② 멘토 2차 검증     현직 실무 멘토 3인이 6단계로 리뷰 (실시간 진행 확인)
      ↓
③ 통합 리포트 배포  AI + 사람 결과 대조 → HTML 다운로드
```

전 과정을 거치면 개인에게는 자신에게 최적화된 성장 솔루션이 남는다. 아카데미·대학교 등 교육 기관에
공급하는 AX 검증 솔루션으로 시작해(B2B), 장기적으로 개인 서비스(B2C)와 해외 시장까지 넓혀가는 것이
로드맵이다.

---

## 사이트 구성

| 탭 | 경로 | 내용 |
|---|---|---|
| **HOME** | `/` | Melius형 **무한 확산 이미지 히어로** · 서비스 소개 |
| **PORTFOLIO 분석** | `/portfolio` | 인트로 → 3단계 분석 마법사 |
| **AlignX AI** | `/alignx` | 분석 모델 소개 · 10대 원칙 상세 · 파이프라인 |
| **MY** | `/my` | 역할별로 다른 화면 — 멘티는 진행 상태·회차 카드(3회 이상 시 이력 분석 해금), 멘토는 배정된 검증 요청 큐(`/my/review/:id`에서 채점). 로그인 계정 공통으로 `/my/notifications`에서 알림 전체 확인 |
| **ABOUT** | `/about` | 서비스 소개 · 검증 멘토(직군별 6인) · 문의 |
| **ADMIN** | `/admin` | 암호 인증(`portfolio2026`) · 제출 현황 · 멘토 피드백 작성 — 탭은 보이지만 실제 접근 통제는 아니다(§ADMIN 접근 참고) |

**포트폴리오 분석 3단계**

| 단계 | 경로 | 핵심 |
|---|---|---|
| 1 · AlignX 1차 검증 | `/portfolio/analyze` | 직군 선택 → 업로드 → 30초 전체화면 로딩(원칙별 클리어 애니메이션 · 진행률 %) → 10원칙 점수 |
| 2 · 멘토 2차 검증 | `/portfolio/mentor` | 문서 자동 첨부 · 이름/주제/요청사항 3항목 → 실시간 검증 모니터 |
| 3 · 통합 리포트 | `/portfolio/report` | AI ↔ 멘토 원칙별 대조 · 종합 점수 · **결과 리포트를 HTML로 배포(다운로드)** · 리포트 확인 후 만족도 7점 척도 3항목 + 주관식 후기로 최종 제출 |

---

## AI 10대 원칙

기획자·PM·마케터·디자이너부터 코드리뷰까지 — 직무에 관계없이 적용되도록 설계된 채점 기준이다.

| # | 원칙 | English | 배점 |
|---|---|---|---|
| 1 | 정보 위계 | Information Hierarchy | 10 |
| 2 | 구조적 정합성 | Structural Alignment | 10 |
| 3 | 명료한 표현 | Clarity of Expression | 10 |
| 4 | 강조와 대비 | Emphasis & Contrast | 10 |
| 5 | 여백과 완급 | Pacing & Space | 10 |
| 6 | 일관성 | Consistency | 10 |
| 7 | 정보 구조 · 스토리텔링 | Structure & Narrative | 10 |
| 8 | 문제 정의 · 근거 | Problem Framing & Evidence | 10 |
| 9 | 실행 가능성 · 사용성 | Feasibility & Usability | 10 |
| 10 | 결과 · 임팩트 증명 | Outcome & Impact | 10 |

총점 = 단순 합산(0–100) · 등급 `S 90+ / A 80–89 / B 70–79 / C 60–69 / D <60`

> ⚠️ **MVP의 점수는 실제 AI 추론이 아니라 결정론적 더미 생성기의 출력이다.** 파일 지문을 시드로 쓰므로 같은 파일은 항상 같은 점수가 나온다. 실제 AlignX 모델은 [`AnalysisProvider` 인터페이스](Plans/05-portfolio-step1.md) 한 줄 교체로 연결된다.

---

## 기술 스택

| 영역 | 선택 |
|---|---|
| 빌드 | Vite 6 |
| UI | React 19 · TypeScript |
| 라우팅 | React Router v7 (`BrowserRouter` + `basename`) |
| 상태 | Zustand + persist (localStorage) |
| 스타일 | **CSS Custom Properties + CSS Modules** — 디자인 시스템이 토큰 참조만 허용 |
| 모션 | motion · Lenis |
| PDF | pdfjs-dist |
| 테스트 | Vitest · Testing Library |
| 배포 | GitHub Pages + GitHub Actions |

**백엔드가 없다.** 모든 데이터는 브라우저 `localStorage` 에만 저장된다.

---

## 디자인 시스템

기준 문서: [`woongdesignv2.md`](woongdesignv2.md)

- **캔버스** 따뜻한 오프화이트 `#FCFCFF` ↔ 잉크 `#010102` (순백·순흑 미사용)
- **4개 톤 세트** — Cool(Blue/Mint) = 도구·데이터 / Warm(Orange/Violet) = 대화·감성. **한 페이지엔 한 세트만**
- **폰트** Montserrat(영문 디스플레이, weight 300) + Pretendard(한글 본문, weight 400) — 자체 호스팅
- **그림자 없음** — 깊이는 표면 색 단계(canvas → tint/soft → deep)로만
- **이모지 · 일러스트 · 코어 그라디언트 없음**
- 카드 라운드 **28px**(시그니처) · 여백 리듬 24 / 48 / 80
- weight 700은 **페이지당 한 번**, 임팩트 모먼트에만

### 라우트별 톤 매핑

| 라우트 | 톤 |
|---|---|
| `/`, `/portfolio`, `/portfolio/analyze`, `/portfolio/report`, `/admin` | Cool · Blue |
| `/portfolio/mentor` | Warm · Orange |
| `/alignx` | Cool · Mint |
| `/my` | Warm · Orange |
| `/about` | Warm · Violet |

### 라이트 / 다크 모드

상단바 **우측 고정** 아이콘으로 전환. 초기값은 `localStorage` → 없으면 시스템 설정. 인라인 부트 스크립트로 깜빡임(FOUC) 방지.

### 원본 대비 의도적 이탈 3건

| 항목 | woongdesignv2 | 이 프로젝트 | 사유 |
|---|---|---|---|
| 디스플레이 폰트 | Samsung Sharp Sans | **Montserrat** | 프로젝트 요구 |
| 사진 · 일러스트 | 사용 안 함 | **썸네일 적극 사용** | 히어로·콘텐츠 요구 |
| 본문 크기 | 24px | **웹 스케일 18px** | 슬라이드(1920×1080) 수치를 웹 본문에 그대로 쓰면 가독성 붕괴 |

### 폰트 구성

`public/fonts/` 에 woff2 4개, 약 856KB를 자체 호스팅한다. CDN 의존 없음.

| 파일 | 커버 | 크기 |
|---|---|---|
| `Montserrat-latin.woff2` | Variable(wght 100–900) · **라틴 전용** `unicode-range` | ~50KB |
| `Pretendard-{Regular,Medium,Bold}.subset.woff2` | 한글 400 / 500 / 700 | 각 ~267KB |

- Montserrat Variable 하나로 디스플레이 300 · 카드 500 · 임팩트 700을 모두 커버한다
- 덕분에 `woongdesignv2.md` 가 미해결로 남긴 **"디스플레이 Light(300) 파일 부재"** 문제가 해소된다
- Montserrat의 `unicode-range` 라틴 한정이 핵심이다 — 없으면 한글까지 Montserrat이 매칭돼 Pretendard가 로드되지 않는다
- preload는 Pretendard Regular + Montserrat 2개만. 초기 폰트 전송량 350KB 이하가 목표
- 두 폰트 모두 **OFL 1.1** — 라이선스 원문을 `public/fonts/` 에 함께 배포한다 (재배포 조건)

원본 `Font/` 74MB는 저장소에 커밋하지 않는다. 변환 산출물만 커밋한다.

---

## 로컬 실행

```bash
npm install
npm run assets     # Thumbnails / Portfolio-samples → 최적화 에셋 생성 (최초 1회)
npm run dev
```

| 스크립트 | 내용 |
|---|---|
| `npm run dev` | 개발 서버 |
| `npm run build` | 타입 체크 + 프로덕션 빌드 |
| `npm run preview` | 빌드 결과 미리보기 |
| `npm run test` | Vitest |
| `npm run lint` | ESLint (`--max-warnings 0`) |
| `npm run assets` | 폰트 변환 + 이미지 최적화 + PDF 프리뷰 추출 |

> `npm run assets` 는 필수다. 원본 에셋이 **328MB**(썸네일 44MB + 샘플 PDF 210MB + 폰트 74MB)라 그대로는 배포할 수 없다. 스크립트가 폰트 서브셋·WebP 변환·PDF 1페이지 추출을 수행해 `public/` 을 8MB 이하로 만든다.
>
> 폰트 변환은 최초 1회만 필요하고 결과가 커밋되므로, 이후 실행에서는 자동으로 건너뛴다.

---

## 썸네일 교체 방법

콘텐츠 썸네일은 **코드를 건드리지 않고** 교체할 수 있다.

```
public/images/
├─ hero/          # HOME 히어로 이미지
├─ thumbs/        # 각 페이지 섹션 썸네일 ← 여기를 교체
├─ brand/         # 로고
├─ manifest.json  # 파일명 → 표시 위치 · alt 텍스트 매핑
└─ README.md      # 교체 규칙
```

1. `public/images/thumbs/` 의 파일을 **같은 이름으로 덮어쓴다**
2. `npm run assets` 실행
3. 끝 — 코드 수정 불필요

권장: 16:10 비율 · 원본 폭 2400px 이상 · 최적화 후 300KB 이하.
컴포넌트는 파일 경로를 하드코딩하지 않고 `manifest.json` 을 통해서만 이미지를 참조한다.

---

## ADMIN 접근

`/admin` → 암호 입력 (`portfolio2026`) · 눈 아이콘 토글로 입력값 표시/숨김

상단 로그인 아이콘에서 "관리자" 데모 계정으로 전환하면 내비게이션에 ADMIN 탭이 보이지만, 이는 탭 노출일 뿐 실제 접근 통제가 아니다 — `/admin`은 계정과 무관하게 누구나 주소로 직접 들어가 암호만 맞으면 통과한다(§보안 고지).

**할 수 있는 일**
- 전체 포트폴리오 제출 현황 조회 (상태 필터 · 검색 · 정렬)
- 멘토 요청 건에 대한 실제 피드백 작성 (원칙별 코멘트 · 종합 코멘트 · 멘토 점수)
- 검증 단계 수동 진행 / 즉시 완료 처리
- `Portfolio-samples/` 기반 샘플 10건으로 화면 확인

### ⚠️ 보안 고지

**이 암호는 실제 접근 통제가 아니다.** 프론트엔드 번들에 평문으로 포함되므로 브라우저 개발자도구에서 누구나 볼 수 있다. 데모용 화면 구분 장치로만 취급한다.

또한 ADMIN은 **같은 브라우저의 `localStorage` 데이터만** 조회한다. 다른 기기에서 제출된 내용은 원리적으로 볼 수 없다.

실제 운영에는 서버 측 인증이 필수다.

---

## 알려진 제약

| 제약 | 내용 |
|---|---|
| 백엔드 없음 | 모든 데이터가 브라우저에만 저장된다. 기기·브라우저를 바꾸면 사라진다 |
| AI 점수는 더미 | 결정론적 시뮬레이션. 실제 모델 추론이 아니다 |
| 멘토 검증은 실제 멘토 계정이 담당 | 접수·배정만 타이머 연출이고, 실제 채점은 멘토 계정 로그인 후 직접 작성해야 완료된다(ADMIN 대행도 가능) |
| 저장 용량 5MB | 업로드 원본은 저장하지 않고 640px 프리뷰 + 전 페이지(최대 10장) 축소본만 보관. 용량이 부족하면 완료된 회차부터 단계적으로 정리 |
| 실기기 데이터 공유 불가 | 멘티·멘토·관리자 계정 전환도 **같은 브라우저의 localStorage** 안에서만 일어난다. 다른 기기의 멘토는 이 요청을 볼 수 없다 |

---

## 배포 (GitHub Pages)

`main` 브랜치에 push하면 `.github/workflows/deploy.yml`이 `npm run assets` → `lint` → `test` →
`build` → `deploy` 순서로 실행된다. 검수를 통과하지 못한 코드는 이 파이프라인 자체가 막는다.

```
https://woongs2021.github.io/AlignX/
```

**설정**
1. 저장소 Settings → Pages → Source = **GitHub Actions**
2. `vite.config.ts` 의 `base`(`/AlignX/`)와 `src/router.tsx` 의 `BASENAME`이 저장소명과 일치해야
   한다 — 배포 후 흰 화면이나 전 에셋 404가 난다면 이 둘부터 확인한다
3. `public/.nojekyll` 유지 — 없으면 Jekyll이 Vite의 `_`로 시작하는 산출물을 무시해 404가 난다
4. `public/404.html` + `index.html`의 복원 스크립트가 SPA 딥링크(`/portfolio/analyze` 등 직접
   접속·새로고침)를 지원한다

자세한 절차: [Phase 13](Plans/13-deploy-docs.md)

---

## 디렉터리 구조

```
AlignX/
├─ .github/workflows/      # GitHub Pages 배포 워크플로
├─ Plans/                  # 구현 계획 (Phase 00–13) + reports/ 검수 산출물
├─ public/
│  ├─ fonts/  images/  samples/
│  ├─ 404.html  .nojekyll  robots.txt
├─ scripts/                # 에셋 최적화
├─ src/
│  ├─ styles/              # tokens.css — hex가 존재하는 유일한 파일
│  ├─ layout/  pages/  components/
│  ├─ features/            # analysis · mentor · report
│  ├─ store/  data/  lib/  types/
├─ Thumbnails/             # 원본 이미지 (소스, 커밋)
├─ Portfolio-samples/      # 원본 PDF (gitignore — 210MB)
├─ Font/                   # 원본 폰트 (gitignore — 74MB, 산출물만 커밋)
├─ AlignX/  References/
├─ CLAUDE.md               # 작업 원칙
├─ woongdesignv2.md        # 디자인 시스템
└─ README.md
```

---

## 구현 계획

| Phase | 문서 | 내용 |
|---|---|---|
| 00 | [개요 · 전제 · 의사결정](Plans/00-overview.md) | 미해결 질문 7건 · 데이터 모델 · 톤 매핑 · 에셋 인벤토리 |
| 01 | [프로젝트 셋업 · 인프라](Plans/01-setup-infra.md) | 스캐폴드 · 라우팅 · 스토어 · 에셋 파이프라인 |
| 02 | [디자인 시스템 토큰화](Plans/02-design-tokens.md) | 컬러 4세트 · 웹 타입 스케일 · 다크 모드 |
| 03 | [레이아웃 · 내비게이션](Plans/03-layout-navigation.md) | TopNav · 탭 호버 · 스크롤 시스템 · Footer |
| 04 | [HOME](Plans/04-home.md) | 무한 확산 히어로 · 성능 가드 |
| 05 | [분석 1단계](Plans/05-portfolio-step1.md) | 업로드 · 로딩 · 10원칙 스코어링 · 모델 교체 경계면 |
| 06 | [분석 2단계](Plans/06-portfolio-step2.md) | 멘토 제출 폼 · 7점 척도 · 실시간 검증 모니터 |
| 07 | [분석 3단계](Plans/07-portfolio-step3.md) | 통합 리포트 · HTML 다운로드 |
| 08 | [AlignX AI · ABOUT](Plans/08-alignx-about.md) | 모델 소개 · 서비스 소개 · FAQ |
| 09 | [MY](Plans/09-my.md) | 회차 분기 · 이력 분석 · 성장 차트 |
| 10 | [ADMIN](Plans/10-admin.md) | 암호 게이트 · 제출 현황 · 피드백 작성 |
| 11 | [반응형 · 접근성 · 성능](Plans/11-responsive-a11y-perf.md) | WCAG AA · Lighthouse 목표 |
| 12 | [**최종 코드 검수**](Plans/12-code-review.md) | 자동 검사 · 요구사항 대조 · E2E 시나리오 |
| 13 | [배포 · 문서화](Plans/13-deploy-docs.md) | GitHub Pages · Actions · README |

---

## 계획 단계에서 정리한 쟁점

원문 요구가 갈리거나 에셋이 비어 있던 지점을 계획 단계에서 정리하고 아래 가정으로 구현했다.

| # | 쟁점 | 가정 |
|---|---|---|
| 1 | "4개 탭"이라 했으나 5개가 나열됨 | 로고(HOME) + 내비 탭 4개(PORTFOLIO 분석/AlignX AI/MY/ABOUT)로 구현. 이후 ADMIN도 탭으로 노출해달라는 요청에 따라 현재는 5개다 |
| 2 | "Home은 기본적인" 문장이 끊김 | 홍보 화면으로 해석 |
| 3 | "무한으로 넓게 퍼지는" 히어로 해석 | 무한 가로 마퀴 + 스크롤 확산 (대안 B안 병기) |
| 4 | MY 이력 분석 해금 "n회" | **3회** |
| 5 | AlignX AI 영상 에셋 미제공 | 로고 SVG 모션으로 대체, `<video>` 슬롯 유지 |
| 6 | 폰트 바이너리 부재 (P0) | ✅ **해결** — Montserrat Variable TTF · Pretendard woff2 전 세트 확보 |
| 7 | ADMIN 탭 노출 후 게이트 화면이 다른 탭과 시각적으로 이질적 | 기존 `--deep` 풀스크린 카드 레이아웃을 다른 탭과 동일한 `container` + `SectionHeader` + `Card`/`Button` 구조로 재구성. 인증 로직(눈 토글·잠금·aria-live)은 변경 없음 |

## 남은 작업 (배포 후 후속)

- [ ] OG 이미지(`public/og.png`, 1200×630) 제작 — 현재 메타태그는 경로만 참조하고 있어 이미지 없이는 링크 미리보기에 썸네일이 뜨지 않는다
- [ ] 실기기(iOS Safari·Android Chrome) 확인, 배포본 기준 Lighthouse 재측정 — `Plans/reports/lighthouse.md`·`a11y-axe.md` 참고
- [ ] 스크린샷 3장 촬영 후 위 섹션 교체

---

## 작업 원칙

이 저장소의 모든 작업은 [`CLAUDE.md`](CLAUDE.md) 를 따른다.

1. **Think Before Coding** — 가정을 명시하고, 해석이 갈리면 묻는다
2. **Simplicity First** — 요청 범위를 넘는 추상화·설정 가능성을 만들지 않는다
3. **Surgical Changes** — 변경된 모든 줄이 요구사항으로 추적되어야 한다
4. **Goal-Driven Execution** — 각 Phase는 검증 가능한 완료 기준을 가진다

---

## 크레딧 · 라이선스

- 디자인 시스템: [`woongdesignv2.md`](woongdesignv2.md)
- 레퍼런스: [melius.com](https://www.melius.com/) (히어로 인터랙션) · [seed-design.io](https://seed-design.io/) (내비게이션 · 콘텐츠 구조)
- 폰트: [Montserrat](https://github.com/JulietaUla/Montserrat) (OFL 1.1) · [Pretendard](https://github.com/orioncactus/pretendard) (OFL 1.1) — 라이선스 원문은 `public/fonts/` 에 동봉
- 샘플 포트폴리오: 교육 목적 · 저장소 미포함 · 화면의 학생 이름은 전부 가상

---

*AlignX는 현재 MVP 데모입니다. 분석 결과는 참고 지표이며 최종 판단은 사용자에게 있습니다.*
